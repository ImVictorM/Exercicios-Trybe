type bird = {
  especies: string,
  description: string,
  liveExpectancy: number,
};

type sum = (n1: number, n2:number ) => number; 

type address = {
  number: number,
  city: string,
  state: string
}
